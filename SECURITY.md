# 安全策略

## 支持范围

安全修复优先覆盖最新 GitHub Release 和 `main` 分支。实验性的 Windows/Avalonia 端口按维护能力尽力处理。

## 私下报告

请不要为以下问题创建公开 Issue：

- 能读取或泄露 Claude/Codex 凭据、生日、倒数日、持仓或备份内容；
- 可以执行未授权命令、写入任意路径或绕过用户确认；
- 发布流程、GitHub Actions 或更新机制中的供应链风险；
- 仍然有效的访问令牌或其他凭据。

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。报告中请包含受影响版本、复现步骤、影响范围和建议修复方式；不要附带真实账号数据，尽量使用脱敏样例。

维护者会尽快确认报告，在修复发布前避免公开细节。普通崩溃、界面问题和功能建议请使用 Issue 模板。

## 使用者注意事项

- 只从本仓库的 GitHub Releases 或自行构建的源码安装。
- macOS 社区构建未使用 Developer ID 且未经 Apple 公证；安装前必须核对 Release 提供的 SHA-256。无法确认来源时不要绕过 Gatekeeper。
- `.coolrun` 备份未加密，不要上传到公开 Issue。
- AI 监控会读取本机 CLI 登录状态；共享日志和截图前请检查账号、路径与配额信息。
- 金价和分析结果仅供信息参考，不构成投资建议。
